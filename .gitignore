node_modules/
dist/
coverage/
*.tsbuildinfo

# Environment. .env.example IS committed; real values never are.
.env
.env.*
!.env.example

# Working notes for the reflective document -- deliberately untracked.
NOTES.md

# Generated. Regenerated by `npm run openapi:generate`, but committed so CI
# can diff it against a fresh build and catch a drifted contract.
!openapi.json

logs/
*.log
npm-debug.log*
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Docker secrets, never committed.
secrets/
